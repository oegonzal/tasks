import styled from 'styled-components';

interface Props { }

export const StyledPrivateRoutePage = styled.div<Props>`
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 20px;
    width: 100%;
    max-height: 100%;
    height: 100%;
`;